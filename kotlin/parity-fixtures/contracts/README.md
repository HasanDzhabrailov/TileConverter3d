# Contracts

Place locked contract fixtures here.

Required contract snapshots:

- CLI parse and validation cases
- HTTP request and response payloads
- WebSocket event transcripts
- TileJSON documents
- `style.json` documents
- uploaded MBTiles metadata and style responses
