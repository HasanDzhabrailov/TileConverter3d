# Goldens

Place locked expected outputs here.

Save the smallest stable artifact that still proves parity:

- exact PNG bytes when stable
- decoded RGBA snapshots when PNG ancillary metadata is not stable
- normalized SQL row snapshots for MBTiles metadata and tiles
- exact filesystem layout snapshots for CLI output contracts
