# Inputs

Place locked raw inputs here.

Required groups:

- HGT filename parsing cases
- valid `1201` and `3601` HGT files
- malformed and truncated HGT files
- mixed-resolution directory inputs
- render and sampling inputs with known edge, seam, and void behavior
- uploaded MBTiles examples for raster, raster-dem, malformed metadata, and missing metadata
