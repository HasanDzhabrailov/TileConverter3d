# Core MBTiles Inputs

This directory describes the HGT-to-MBTiles input class protected by the core parity harness.

Current tests lock the normalized MBTiles metadata rows and tile rows produced by `MbtilesWriter`.

Committed raw HGT-to-MBTiles baseline artifacts can be added here later if broader end-to-end MBTiles golden coverage is required.
